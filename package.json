{
  "name": "cypressbase",
  "version": "1.0.0",
  "description": "cypress Base",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:run_spec":"npx cypress run --spec 'cypress-starter/cypress/integration/Assignments/Amazon_Assignment.spec.Ts'",
    "cy:run_browser":"npm run cy:run_spec -- --browser chrome & npm run cy:run_spec -- --browser electron"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^4.4.1"
  },
  "dependencies": {
    "typescript": "^3.9.10"
  }
}
